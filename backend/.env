# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# JWT Configuration
JWT_SECRET=scalable-webapp-super-secret-jwt-key-2024-change-in-production
JWT_EXPIRE=24h

# Database Configuration (optional - will use in-memory storage if not available)
MONGODB_URI= 
